{
  "name": "domotic",
  "version": "2.0.0",
  "description": "A packaged foo fooer for fooing foos",
  "dependencies": {
    "mongodb": "^2.2.12",
    "node-hue-api": "^2.4.2",
    "node-rest-client": "^3.1.0",
    "xml2json": "^0.10.0"
  },
  "devDependencies": {
    "@types/node": "^9.6.2",
    "typescript": "^2.8.1"
  },
  "scripts": {
    "start-radio-only-if-present": "let TocToc = require('/home/pi/domotic2/src/toctoc/toctoc.js').TocToc; new TocToc().updatePresence()"
  }
}
